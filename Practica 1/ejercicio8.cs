/*8) Sea st una variable de tipo string correctamente declarada. ¿Es válida la siguiente instrucción:
Console.WriteLine(st=Console.ReadLine());?*/

string st;

Console.WriteLine(st = Console.ReadLine());

/* Mientras antes se haya declarado una variable string st es válido. Lo que hace es pedirme que ingrese un string
y automáticamente escribirlo abajo */